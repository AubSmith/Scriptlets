# Creates "local" install
# Packages are installed only for project

# Install virtual environment
pip install virtualenv

# Windows
python -m venv <folder_name>

# OSX/Linux (bash)
virtualenv <folder_name>

# Run Activate shell script

# Virtual environment same as global install