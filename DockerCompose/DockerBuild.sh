docker build
docker run

# Docker Compose
docker-compose up

# Terminate container
Ctrl C 


# Run in background using detached mode
docker-compose up -d

# View Docker processes
docker ps

# Stop containers
docker-compose stop