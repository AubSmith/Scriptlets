#include <iostream>
using namespace std;

int main() {
	cout << "Size of char : " << sizeof(char) << endl; // endl = Flush output buffer immediately
	cout << "Size of int : " << sizeof(int) << endl; // endl = Flush output buffer immediately
	cout << "Size of short int : " << sizeof(short int) << endl; // endl = Flush output buffer immediately
	cout << "Size of long int : " << sizeof(long int) << endl; // endl = Flush output buffer immediately
	cout << "Size of float : " << sizeof(float) << endl; // endl = Flush output buffer immediately
	cout << "Size of double : " << sizeof(double) << endl; // endl = Flush output buffer immediately
	cout << "Size of wchar_t : " << sizeof(wchar_t) << endl; // endl = Flush output buffer immediately

	return 0;
}